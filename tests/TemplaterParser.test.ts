const app = require("app");
import { InternalTemplateParser } from "InternalTemplates/InternalTemplateParser";

test("Get cursor location", () => {
    /*
    let template_parser = new TemplateParser(null, null);
    let content = "test\ntest{{tp.cursor}}\ntest";

    expect(template_parser.get_cursor_location(content)).toBe({line: 1, ch: 5});
    */
});